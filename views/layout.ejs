<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title><%= locals.title ? locals.title : "Inventory Management" %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .navbar-nav .nav-link.active {
      font-weight: bold;
      border-bottom: 2px solid #0d6efd;
    }
  </style>
</head>

<body class="bg-light">

  <!-- ✅ Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top shadow-sm">
    <div class="container-fluid">
      <a class="navbar-brand fw-bold" href="/inventory">Inventory App</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link <%= page === 'items' ? 'active' : '' %>" href="/items">All Items</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <%= page === 'plants' ? 'active' : '' %>" href="/plants">All Plants</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <%= page === 'inventory' ? 'active' : '' %>" href="/inventory">Inventory</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <%= page === 'plantsInventory' ? 'active' : '' %>" href="/inventory/plants">Plant wise Inventory</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <%= page === 'search' ? 'active' : '' %>" href="/inventory/search">Search</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- ✅ Page Content -->
  <div class="container py-4">
    <%- body %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
